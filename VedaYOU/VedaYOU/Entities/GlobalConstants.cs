using VedaYOU.Persistence.DocumentTypes;

namespace VedaYOU.Entities
{
    public static class GlobalConstants
    {
        public static string ArticlesFolderAlias = typeof (ArticlesFolder).Name;
        public static string ArticleAlias = typeof(Article).Name;
    }
}